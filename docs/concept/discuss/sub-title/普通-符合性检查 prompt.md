## 🧑‍💻 User


我需要你进行输出成果的符合性检查。首先给你指令。随后分批次提供输出结果

---
## 🤖 Assistant


我已收到您的指令。我理解我的任务是：

**作为符合性检查Agent，对您即将提供的输出结果进行符合性检验。**

我将依据您提供的这份**"医疗建筑技术系统CIM建模 - 完整Agent体系"**文档作为检验基准，核对后续分批提供的输出成果是否符合以下要求：

---

## 我将执行的符合性检查维度

### 1. **结构符合性检查**
- 输出是否遵循各Agent规定的输出结构（`meta`、各领域模块等）
- YAML格式是否正确、层级是否清晰
- 必选字段是否完整

### 2. **内容符合性检查**
- 输出内容是否覆盖Prompt中要求的各项任务
- 分类体系是否完整（如设备分类、空间分类、流动分类等）
- 模型深度是否满足要求（如P0级设备是否详细建模）

### 3. **依赖关系符合性检查**
- 各Agent的输出是否正确引用了上游Agent的定义
- 节点ID、设备ID、空间ID等是否保持一致
- 跨Agent的映射关系是否正确建立

### 4. **规范符合性检查**
- 命名规范是否一致（如`system_id`、`equipment_type_id`等）
- 枚举值是否在定义范围内
- 单位是否正确使用

### 5. **医疗场景特殊性检查**
- 是否覆盖医疗建筑特殊场景（手术室、ICU、负压病房等）
- 医疗气体、洁净空调等特殊系统是否完整
- 生命安全相关系统是否得到适当处理

---

## 检查流程

当您分批提供输出结果时，我将：

1. **确认对应Agent**：识别该输出对应哪个Agent（Agent-01至Agent-09）
2. **逐项核对**：按照该Agent的Prompt要求逐一检查
3. **标注问题**：清晰标注符合项（✓）与不符合项（✗）
4. **给出建议**：针对不符合项提供改进建议
5. **汇总报告**：完成所有批次后，提供整体符合性评估报告

---

**请开始提供第一批次的输出结果，我将立即开始符合性检查。**

请注明该批次对应的Agent编号（如"Agent-01输出"），以便我准确对照相应的Prompt进行检验。

---